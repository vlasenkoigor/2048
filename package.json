{
  "name": "2048",
  "version": "1.0.0",
  "scripts" : {
    "dev:server" : "npm run dev --prefix server",
    "dev:client" : "npm run start --prefix 2048",
    "build:client" : "npm run build --prefix 2048",
    "build" : "npm run build --prefix server",
    "start" : "npm run start --prefix server"
  },
  "dependencies": {

  },
  "devDependencies": {
  }
}
